name: Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Generate custom-palette snake SVGs
        uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: khaatriinaa
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=%23CBD5E1&color_dots=%23161b22,%231e293b,%23334155,%234f46e5,%236366f1
            dist/github-contribution-grid-snake-dark.svg?color_snake=%23F8FAFC&color_dots=%230d1117,%231e293b,%23334155,%234f46e5,%236366f1

      - name: Push SVGs to the "output" branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
